spring:
  application:
    name: ti-gateway-kubernetes-test
  cloud:
    gateway:
      kubernetes:
        ingress:
          enabled: false

logging:
  level:
    root: INFO
    ti.gateway.kubernetes: DEBUG
    # 抑制测试中预期的错误日志（这些是测试无效输入时的正常行为）
    ti.gateway.kubernetes.jwt.JwtKeyGatewayFilterFactory: WARN
    ti.gateway.kubernetes.jwt.JwtHelper: WARN
